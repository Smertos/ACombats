import { Item } from './item'

export interface Equipped {
  head?: Item,
  body?: Item,
  pants?: Item,
  boots?: Item,
  necklace?: Item,
  gloves?: Item,
  leftRing?: Item,
  rightRing?: Item,
  weapon?: Item
}

